# ValidateUtil - 字符串验证工具


## 1. validate(str: string, pattern: RegExp | string)

校验字符串是否符合正则表达式

参数
- str 字符串 
- pattern 正则表达式，可以是字符串或RegExp类型


## 2. 其他指定的验证方法

```js
Baitu.ValidateUtil.isDate("2023-03-11");
// 输出：true
```
